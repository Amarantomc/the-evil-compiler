use std::str::FromStr;
use crate::nodes::{
    binaryop_node::{BinaryOp, BinaryOpNode},
    block_node::BlockNode,
    destassing_node::DestAssignNode,
    for_node::ForNode,
    funcall_node::FunCallNode,
    function_decl_node::FunctionDecl,
    if_node::IfNode,
    instantiation_node::InstantiationNode,
    let_node::LetNode,
    literal_node::{Literal, LiteralNode},
    member_access_node::{MemberAccessNode, MethodCallNode},
    program_node::{Program, Statement},
    type_decl_node::{TypeDeclNode, AttributeNode, TypeBodyItem},
    typedexpr_node::{Expr, HulkType, TypedExpr},
    unaryop_node::{UnaryOp, UnaryOpNode},
    while_node::WhileNode,
};

grammar;

pub Program: Program = {
    <stmts:Statement+> => Program::new(stmts),
};

Statement: Statement = {
    <FunctionDecl> => Statement::FunctionDecl(<>),
    <TypeDecl> => Statement::TypeDecl(<>),
    <expr:TopExpr> => Statement::Expression(expr),
};

// Top level: SimpleExpr requiere ; u BlockExpr tiene ; opcional
TopExpr: TypedExpr = {
    <e:SimpleExpr> ";" => e,
    <e:BlockExpr> (";")?  => e
};

FunctionDecl: FunctionDecl = {
    "function" <name:Ident> "(" <params:Comma<Param>> ")" "=>" <body:ExprRule> ";" => {
        FunctionDecl::new(name, params, body)
    },
    "function" <name:Ident> "(" <params:Comma<Param>> ")" <body:BlockExpr> (";")?  => {
        FunctionDecl::new(name, params, body)
    },
};

MethodDecl: FunctionDecl = {
    <name:Ident> "(" <params:Comma<Param>> ")" "=>" <body:ExprRule> ";" => {
        FunctionDecl::new(name, params, body)
    },
    <name:Ident> "(" <params:Comma<Param>> ")" <body:BlockExpr> (";")?  => {
        FunctionDecl::new(name, params, body)
    },
};

TypeDecl: TypeDeclNode = {
    "type" <name:Ident> <params:("(" <Comma<Param>> ")")?> "{" <body:TypeBodyItem*> "}" => {
        let mut attributes = Vec::new();
        let mut methods = Vec::new();
        for item in body {
            match item {
                TypeBodyItem::Assignment(a) => attributes.push(a),
                TypeBodyItem::FunctionDecl(m) => methods.push(m),
            }
        }
        TypeDeclNode::new(name, params.unwrap_or(vec![]), attributes, methods)
    }
};

TypeBodyItem: TypeBodyItem = {
    <id:Ident> <ty:TypeAnnotation?> "=" <expr:OrExpr> ";" => {
        TypeBodyItem::Assignment(AttributeNode::new(id, ty.unwrap_or(HulkType::Unknown), expr))
    },
    <MethodDecl> => TypeBodyItem::FunctionDecl(<>),
};
 

 

Param: (LiteralNode, HulkType) = {
    <id:Ident> <ty:TypeAnnotation?> => (id, ty.unwrap_or(HulkType::Unknown))
};

TypeAnnotation: HulkType = {
    ":" <Type> => <>
};

Type: HulkType = {
    "Number" => HulkType::Number,
    "String" => HulkType::String,
    "Boolean" => HulkType::Bool,
    <id:Ident> =>  HulkType::Class(id.value.as_id())
};


Comma<T>: Vec<T> = { 
    <mut v:(<T> ",")*> <e:T?> => match e { 
        None => v,
        Some(e) => {
            v.push(e);
            v
        }
    }
};

// Expresión pura (sin ; manejo): usado dentro de Let, If, While, For
pub ExprRule: TypedExpr = {
    SimpleExpr,
    BlockExpr,
};

// Expresiones simples que requieren ; en top level (pero el ; se agrega en TopExpr)
SimpleExpr: TypedExpr = {
    LetExpr,
    IfExpr,
    WhileExpr,
    ForExpr,
    OrExpr,
};

// Bloques que tienen ; opcional en top level
BlockExpr: TypedExpr = {
    "{" <exprs:(BlockItem)*> "}"  => {
        TypedExpr::new(Expr::Block(BlockNode::new(exprs)))
    }
};

// Items dentro de un bloque: pueden ser simples (con ;) o bloques (con ; opcional)
BlockItem: TypedExpr = {
    <e:SimpleExpr> ";" => e,
    <e:BlockExpr> (";")?  => e
};

LetExpr: TypedExpr = {
    "let" <assignments:Comma<Assignment>> "in" <body:ExprRule> => {
        TypedExpr::new(Expr::Let(LetNode::new(assignments, body)))
    },
};

IfExpr: TypedExpr = {
    "if" "(" <cond:OrExpr> ")" <true_branch:ExprRule> <elif_branches:Elif*> "else" <false_branch:ExprRule> => {
        TypedExpr::new(Expr::If(IfNode::new(cond, true_branch, elif_branches, false_branch)))
    },
};

Elif: (TypedExpr, TypedExpr) = {
    "elif" "(" <cond:OrExpr> ")" <body:ExprRule> => (cond, body),
};

WhileExpr: TypedExpr = {
    "while" "(" <cond:OrExpr> ")" <body:ExprRule> => {
        TypedExpr::new(Expr::While(WhileNode::new(cond, body)))
    },
};

ForExpr: TypedExpr = {
    "for" "(" <var:Ident> "in" "range" "(" <iter1:OrExpr> "," <iter2:OrExpr> ")" ")" <body:ExprRule> => {
        let iter = TypedExpr::new(Expr::FunCall(FunCallNode::new(LiteralNode::new(Literal::Id("range".to_string())), vec![iter1, iter2])));
        TypedExpr::new(Expr::For(ForNode::new(var, iter, body)))
    },
};

Assignment: ((LiteralNode, HulkType), TypedExpr) = {
    <id:Ident> <ty:TypeAnnotation?> "=" <expr:OrExpr> => ((id, ty.unwrap_or(HulkType::Unknown)), expr),
};

pub OrExpr: TypedExpr = { 
    <l:OrExpr> <op:OrOpcode> <r:AndExpr> => {
        TypedExpr::new(Expr::Binary(BinaryOpNode::new(l, op, r)))
    },
    AndExpr,
};

OrOpcode: BinaryOp = {
    "|" => BinaryOp::Or,
    "||" => BinaryOp::Or
};

pub AndExpr: TypedExpr = {
    <l:AndExpr> <op:AndOpcode> <r:AssignExpr> => {
        TypedExpr::new(Expr::Binary(BinaryOpNode::new(l, op, r)))
    },
    AssignExpr,
};

AndOpcode: BinaryOp = {
    "&" => BinaryOp::And,
    "&&" => BinaryOp::And
};

AssignExpr: TypedExpr = {
    <id:Term> ":=" <expr:AssignExpr> => {
        TypedExpr::new(Expr::DestAssign(DestAssignNode::new(id, expr)))
    },
    CompExpr,
};

pub CompExpr: TypedExpr = {
    <l:CompExpr> <op:CompOpcode> <r:ArithExpr> => {
        TypedExpr::new(Expr::Binary(BinaryOpNode::new(l, op, r)))
    },
    ArithExpr,
};

CompOpcode: BinaryOp = { 
    "==" => BinaryOp::Equal,
    "!=" => BinaryOp::Dist,
    ">=" => BinaryOp::Gequa,
    "<=" => BinaryOp::Lequa,
    ">" => BinaryOp::Great,
    "<" => BinaryOp::Less
};

pub ArithExpr: TypedExpr = {
    <l:ArithExpr> <op:ArithOpCode> <r:FactAnd> => {
        TypedExpr::new(Expr::Binary(BinaryOpNode::new(l, op, r)))
    },
    FactAnd,
};

ArithOpCode: BinaryOp = { 
    "+" => BinaryOp::Add,
    "-" => BinaryOp::Sub,
};

FactAnd: TypedExpr = {
    <l:FactAnd> <op:FactAndOp> <r:UnaryExpr> => {
        TypedExpr::new(Expr::Binary(BinaryOpNode::new(l, op, r)))
    },
    UnaryExpr,
};

FactAndOp: BinaryOp = {
    "*" => BinaryOp::Mul,
    "/" => BinaryOp::Div,
    "%" => BinaryOp::Mod,
};

UnaryExpr: TypedExpr = {
    <op:UnaryOpCode> <expr:Base> => {
        TypedExpr::new(Expr::Unary(UnaryOpNode::new(op, expr)))
    },
    Base,
};

UnaryOpCode: UnaryOp = {
    "+" => UnaryOp::Plus,
    "-" => UnaryOp::Neg,
    "!" => UnaryOp::Not,
};

Base: TypedExpr = {
    <l:Base> <op:PowOpcode> <r:Term> => {
        TypedExpr::new(Expr::Binary(BinaryOpNode::new(l, op, r)))
    },
    Term,
};

PowOpcode: BinaryOp = {
    "^" => BinaryOp::Pow
};

Term: TypedExpr = {
    Num => TypedExpr::new(Expr::Literal(LiteralNode::new(Literal::Number(<>)))),
    "true" => TypedExpr::new(Expr::Literal(LiteralNode::new(Literal::Bool(true)))),
    "false" => TypedExpr::new(Expr::Literal(LiteralNode::new(Literal::Bool(false)))),
    StringLit => TypedExpr::new(Expr::Literal(LiteralNode::new(Literal::Str(<>)))),
    "new" <id:Ident> "(" <args:Comma<SimpleExpr>> ")" => {
        TypedExpr::new(Expr::Instantiation(FunCallNode::new(id, args)))
    },
    <inst:Term> "." <call:FunCall> => {
        TypedExpr::new(Expr::MethodCall(MethodCallNode::new(inst, call)))
    },
    <inst:Term> "." <member:Ident> => {
        TypedExpr::new(Expr::MemberAccess(MemberAccessNode::new(inst, member)))
    },
    <FunCall> => TypedExpr::new(Expr::FunCall(<>)),
    "(" <ExprRule> ")" ,
    <id: Ident> => TypedExpr::new(Expr::Literal(id))
};

FunCall: FunCallNode = {
    <id:Ident> "(" <args:Comma<SimpleExpr>> ")" => {
        FunCallNode::new(id, args)
    },
};

Num: f32 = {
    r"[0-9]+(\.[0-9]+)?" => f32::from_str(<>).unwrap()
};

StringLit: String = {
    r#""[^"]*""# => <>.trim_matches('"').to_string()
};

Ident: LiteralNode = {
    r"[a-zA-Z_][a-zA-Z0-9_]*" => LiteralNode::new(Literal::Id(<>.to_string())),
};